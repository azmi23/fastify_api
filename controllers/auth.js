'use strict'

module.exports = { 
  addClient: async function(req, res, ctx) {
    const models = ctc.modelsDb
    try {
      const client = models.client.create({
        
      })
    } catch (error) {
      
    }
  }
}